<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assignment.suzume.mapper.GamerMapper">

    <resultMap id="gamerResultMap" type="Gamer">
        <id property="username" column="username" />
        <result property="password" column="password" />
        <result property="win" column="win" />
        <result property="lose" column="lose" />
        <result property="draw" column="draw" />
        <result property="score" column="score" />
    </resultMap>

    <select id="getGamerByUsername" resultMap="gamerResultMap">
        SELECT *
        FROM gamer
        WHERE username = #{username}
    </select>

    <insert id="insertGamer" parameterType="Gamer">
        INSERT INTO gamer (username, password, win, lose, draw, score)
        VALUES (#{username}, #{password}, #{win}, #{lose}, #{draw}, #{score});
    </insert>

    <update id="updateGamer" parameterType="Gamer">
        UPDATE gamer
        SET username = #{username},
        password = #{password},
        win = #{win},
        lose = #{lose},
        draw = #{draw},
        score = #{score}
        WHERE id = #{id}
    </update>

</mapper>